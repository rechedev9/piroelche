<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Piroboom

## Scope and source of truth

- This is a Next.js 16 App Router site for Piroboom. Use Node 24+ and the pinned `pnpm@11.22.0`.
- Before editing, inspect `git status --short`, `README.md`, the relevant code and test, and the applicable document in `docs/`. Preserve unrelated work and stage only the intended files.
- The approved design is implemented in the current UI and recorded in `evidence/`. The original `piroboom-handoff/inputs/diseno/Piroboom Prototipo.dc.html` may exist locally but is not distributed with the repository. Preserve the agreed visual language; do not redesign it by default.
- Read the relevant installed Next guide under `node_modules/next/dist/docs/` before changing Next routes, metadata, route handlers, or configuration. Do not remove or alter the autogenerated block above.
- Keep modules' existing server/client boundaries. `src/lib/content.ts` owns validated editorial data; `src/lib/catalogue.ts` is server-only; `src/lib/catalogue-model.ts` owns pure reader projections/search; `src/lib/lead-*.ts` owns the lead contract, draft lifecycle, client transport and server delivery. See `docs/architecture.md` before changing these contracts.

## Content, public claims, and forms

- `content/site.json` is the editable published content and must pass `pnpm validate:content`. Keep examples, visual fixtures and test data in `fixtures/` or clearly labelled local-review paths; never publish them as business facts.
- Do not invent products, prices, stock, case studies, legal claims, addresses, campaign facts, or form delivery. Preserve the original catalogue PDF and its manifest/hashes. If its source is unavailable, do not generate a substitute commercial catalogue.
- Keep public indexing disabled unless the real-domain release has `PIROBOOM_PUBLIC_SITE=1` and the legal texts pass validation. Respect the metadata, robots, sitemap and canonical rules in `docs/seo.md`; build-time environment values affect the Docker image.
- Lead delivery remains disabled until a real receiver, server-only secrets and a verified receipt path are configured. Do not show a public success state or claim a message was delivered without that receipt; retain the phone alternative. The local receiver (`pnpm demo`) is only for labelled synthetic submissions.
- The prepared catalogue workflow is Canva -> exported selectable-text PDF -> private Drive `catalogo.pdf` -> import in a clean build checkout -> verified deployment. Synchronization is disabled by default; no direct Canva integration or scheduled job is provided. Check actual server configuration before claiming a connection is active. Follow `docs/catalogue-sync.md`; never commit private Drive IDs, service-account JSON, hooks, or other secrets.

## Local work and verification

```powershell
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm exec playwright install chromium firefox webkit
$env:PLAYWRIGHT_BROWSERS_PATH = Join-Path $env:LOCALAPPDATA "ms-playwright"
$env:LOCALAPPDATA = "$PWD/.local"; pnpm test:e2e
```

- Use `pnpm demo` for the local fixture review at `127.0.0.1:3000` (and its private receiver on 4010). Use `pnpm build` then `pnpm start --port 3001` for a public-content production preview. Check ports before starting servers; use the isolated E2E command in `README.md` when they are occupied.
- For UI changes, exercise the affected route at mobile, laptop and desktop widths, including long unbroken content, and verify the relevant keyboard and accessibility flow. Do not weaken tests to hide a failure. Preserve the catalogue importer's Windows-compatible resource paths. Reproduce and report any platform limitation precisely, including browser keyboard behavior.
- Run the checks proportional to the change, inspect the final diff, and state what was actually run. Distinguish local checks, CI, and an installed/deployed result; no local or CI result proves a remote deployment or form reception.

## Deployment and repository operations

- Production is intended for Keko's VPS. Keko will add access, hostname, TLS/reverse-proxy and secret details; none are known in this repository. Do not invent credentials, SSH aliases, IPs, domains, proxy configuration, or a deployment target. The former personal VPS is not a target.
- `deploy/Dockerfile` builds a Node 24 standalone image; `deploy/compose.yaml` runs its read-only `piroelche` container on loopback port 8093. Treat the current deployment files as the implementation to adapt to Keko's supplied host configuration, and read their README before changing them.
- When deployment is authorized and Keko's details exist, build a bounded candidate from a clean checkout, preserve the currently running release and any other VPS applications, back up the deployment configuration needed to roll back, then validate the image, compose configuration, health/HTTP response, canonical URL, robots/indexing state, and catalogue manifest before declaring it live. Change DNS, proxy rules, form secrets, Drive sync, or a scheduler only when those items are in scope and configured.
- Keep credentials, private `.env` files, tokens and unpublished catalogue material out of Git. Review outgoing changes for secrets; inspect relevant history before changing a private repository to public. Remove/rotate any exposed credential. Do not rewrite shared history or delete remote resources without explicit task scope. Once push, public visibility, or deployment has been authorized for the current task, carry that authorization through the concrete operation; do not add repetitive confirmation gates.
